<!DOCTYPE html>
<html>
<head>
  <!-- complete base template -->
  <meta charset="utf-8" />
  <title>{{block "title" .}}Go-SSB Room Server{{end}}</title>
</head>
<body>
  {{block "extra" .}}{{end}}
  <div class="container">
    {{ block "content" . }}{{end}}
  </div>
</body>
</html>