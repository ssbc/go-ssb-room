{{ define "title" }}<title>News - Post</title>{{ end }}
{{ define "content" }}
<div class="page-header">
  <h1>{{.Post.Name}}</h1>
</div>
<div class="row">
<div class="col-md-12">
<p>{{.Post.Text}}</p>
</div>
</div> <!-- /row -->
<a href="{{urlTo "news:overview"}}" id="overview">Overview</a>
<a href="{{urlTo "news:post" "PostID" (inc .Post.ID)}}" id="next">Next...</a>
{{end}}